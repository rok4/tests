meta {
  name: NOK avec VERSION erronée
  type: http
  seq: 2
}

get {
  url: {{endpoint}}/tms/2.0.0
  body: none
  auth: none
}

assert {
  res.status: eq 400
  res.headers["content-type"]: eq text/xml
  res.body: contains "Invalid version (only 1.0.0 available)"
}
