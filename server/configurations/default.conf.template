upstream rok4server { server middle:10000; }
                                               
server {
    listen 80 default_server;

    location / {
        fastcgi_pass rok4server;
        include fastcgi_params;
    }
}